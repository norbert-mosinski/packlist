import { DListCreateView } from "@/presentation/list/components/list-create-view";

export default async function Page() {
    // page is used for routing. Returns just the Dview.
    return <DListCreateView></DListCreateView>
}